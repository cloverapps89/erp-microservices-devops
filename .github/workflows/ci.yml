name: build-and-test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # 🧪 Run service tests from repo root
      - name: Run all service tests from repo root
        run: |
          python -m venv .venv
          source .venv/bin/activate
          pip install -r orders-service/requirements.txt
          pip install -r inventory-service/requirements.txt
          pip install pytest
          PYTHONPATH=$PWD pytest orders-service/tests inventory-service/tests


      # 🛠️ Build Docker images
      - name: Build Docker images
        run: |
          docker build -t inventory-service ./inventory-service
          docker build -t orders-service ./orders-service
